import type { Metadata } from 'next'
import { Inter } from 'next/font/google'
import './globals.css'
import { Providers } from './providers'

const inter = Inter({ subsets: ['latin'] })

export const metadata: Metadata = {
  title: 'Ero Chat - Anonymous Erotic Dating',
  description: 'Connect mentally before physically. Anonymous chat that reveals only when you both match.',
  keywords: ['dating', 'anonymous', 'chat', 'adult', 'connection'],
  authors: [{ name: 'Ero Chat Team' }],
  icons: {
    icon: '/favicon.ico',
  },
}

export default function RootLayout({
  children,
}: {
  children: React.ReactNode
}) {
  return (
    <html lang="en">
      <body className={inter.className}>
        <Providers>
          {children}
        </Providers>
      </body>
    </html>
  )
}
